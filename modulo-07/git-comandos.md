# Comandos Git aprendidos - Módulo 7A

## Ramas
- `git checkout -b nombre-rama` - crea y cambia a rama nueva
- `git branch` - lista ramas, asterisco indica rama actual
- `git branch -d nombre-rama` - borra rama ya fusionada
- `git checkout main` - volver a main

## Sincronización
- `git push origin nombre-rama` - subir rama a GitHub
- `git pull origin main` - traer cambios de GitHub a local

## GitHub CLI
- `gh issue create` - crear issue desde terminal
- `gh issue list` - listar issues abiertos
- `gh pr create` - crear pull request desde terminal
- `gh pr merge` - hacer merge desde terminal